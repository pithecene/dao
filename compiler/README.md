# Compiler

C++23 host compiler implementation. The subsystem map lives in
`docs/ARCH_INDEX.md`; phase boundaries are frozen in
`docs/contracts/CONTRACT_COMPILER_PHASES.md`.
Do not create subtrees here without updating `docs/ARCH_INDEX.md` and the
relevant compiler contract.
