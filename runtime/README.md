# Runtime

Native runtime linked into every Dao executable (`core/`) plus
allocation-domain support (`memory/`). `modes/` and `gpu/` are reserved.
The ABI surface is frozen in `docs/contracts/CONTRACT_RUNTIME_ABI.md`.
