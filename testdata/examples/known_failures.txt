# Examples the compiler cannot build yet, one per line, tab-separated:
# <file>\t<diagnostic the compiler must report>.  playground_service_test
# expects exactly that failure and flags the file once it builds.
raw_memory.dao	MIR concreteness invariant violated
